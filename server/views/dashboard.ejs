<!DOCTYPE html>
<html>
  <head>
    <title>Faculty Recruitment | NIT Trichy</title>
    <link href="css/bootstrap.css" rel="stylesheet">
    <link rel="stylesheet" href="css/font-awesome-4.7.0/css/font-awesome.min.css">
    <script type="text/javascript" src="js/jquery-2.1.1.min.js"></script>
    <script type="text/javascript" src="js/popper.min.js"></script>
    <script type="text/javascript" src="js/bootstrap.min.js"></script>
    
    <style>
        label {
            font-weight: 600;
            font-size: 14px;
            color: #394263;
        }
        .form-group {
            border-bottom: 1px dashed lightblue;
            padding: 5px 0;
            margin: 0 2px;
        }
        .error {
            color: red;
            font-size: 14px;
        }
        .nav-link{
            margin-left: -5px;
            padding:3px 8px;
            font-size:15px;
        }
        .instructions {
            margin:25px;
        }
        li{
            font-size:15px;
        }
        .box-row {
            padding: 10px;
            padding-bottom: -15px;
        }
        .box-row:nth-child(odd) {
            background: lightblue;
        }
        .box-row:nth-child(even) {
            background: rgb(240,240,255);
        }
        .links{
          margin-left:50%;
          width:50%;
        }
        .links a{
          color:white;
          text-decoration: none;
          padding:23px;
        }
        .links a:hover{
          color:#0084FF;
          text-decoration: none;
          transition-duration: 0.5s;
        }
        th, td{
          width:200px;
        }
        .table{
          margin-left:30px;
        }
        .newApplication{
          color:white;
        }
        .newApplication:hover{
          color:black;
          transition-duration: 0.5s;
        }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-dark bg-dark">
      <div>
          <img src="/images/logo.png" style="max-width: 75px">
          <a class="navbar-brand" href="#" style="padding: 10px; font-weight:bold">Faculty Recruitment | NIT Trichy</a>
          <div class="links">
            <a href="/instructions">INSTRUCTIONS</a>
            <a href="/dashboard">DASHBOARD</a>
            <a href="/changePassword">CHANGE PASSWORD</a>
            <a href="/logout">LOGOUT</a>
          </div>
      </div>
    </nav>
    <div class="applications" style="margin-left:30px;">
      <h4 style="padding:30px;"> Hi, <%= user.name%></h4>
      <h5 style="margin-left:30px;">Your applications:</h5>
      <div class="applications" style="margin-left:10px;">
        <table class="table">
            <tr class="row">
              <th style="padding:20px;" >Applied Date</th>
              <th style="padding:20px;" >Status</th>
            </tr>
            <%for(var i=0;i<user.applications.length;i++ ){%>
              <tr class="row">
                <td><%= user.applications[i].appliedDate%></td>
                <td><%= user.applications[i].status%></td>
              </tr>
            <%}%>
          </table>
          <div class="btn btn-primary" style="margin-left:30px;"><a class="newApplication" href="/apply">NEW APPLICATION</a></div>
      </div>
    </div>
  </body>
</html>
